{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["_next/**"]
    },
    "build:package": {
      "outputs": ["dist"]
    },
    "dev": {
      "cache": false
    },
    "lint:js": {}
  }
}
